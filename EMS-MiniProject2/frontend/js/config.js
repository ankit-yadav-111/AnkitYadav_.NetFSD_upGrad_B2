// ============================================================
// config.js
// Purpose : Stores API base URL and page size constant
//           Replaces data.js as the configuration source
//  if does not worls then change it to 5000============================================================

const API_BASE_URL = 'https://localhost:7189/api';
const PAGE_SIZE = 10;